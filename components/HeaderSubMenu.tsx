import React, { FC } from "react";

type Props = {
  label: string;
  links: {
    label: string;
    url: string;
  }[];
};

export const HeaderSubMenu: FC<Props> = (props) => {
  return <div>HeaderSubMenu</div>;
};
